install_subdir(
    'installed',
    install_dir: 'share/www/redfish/v1/schema',
    strip_directory: true,
)

install_subdir('JsonSchema', install_dir: 'share/www/redfish/v1/')

subdir('json-schema')